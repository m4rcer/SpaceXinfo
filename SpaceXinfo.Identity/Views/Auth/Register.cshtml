@model RegisterViewModel

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta itemprop="image" content="https://www.spacex.com/static/images/share.jpg">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href=@Url.Content("~/styles/app.css")>
    <title>Sign Up - SpaceXinfo</title>
</head>
<body>
    <main class="main">
        <div class="register">
            <div class="container">
                <div class="register__inner">
                    <div class="register__circle"></div>
                    <div class="register__form__container">
                        <form asp-action="Register" asp-controller="Auth" method="post">
                            <input type="hidden" asp-for="ReturnUrl" />
                            <h1 class="register__form__title">
                                Sign Up
                            </h1>
                            <div class="register__form__fields">
                                <div class="register__form__field">
                                    <label for="username" class="register__label">Nickname</label>
                                    <input class="register__input" 
                                    type="text"
                                    placeholder="Nickname" 
                                    required
                                    asp-for="Username"
                                    id="username">
                                    <span asp-validation-for="Username"></span>
                                </div>
                                <div class="register__form__field">
                                    <label for="password" class="register__label">Password</label>
                                    <input class="register__input" 
                                    asp-for="Password" 
                                    placeholder="Password" 
                                    required minlength="8" 
                                    maxlength="24"
                                    id="password">
                                    <span asp-validation-for="Password"></span>
                                </div>
                                <div class="register__form__field">
                                    <label for="repeat_password" class="register__label">Repeat password</label>
                                    <input class="register__input"
                                    asp-for="ConfirmPassword" 
                                    placeholder="Confirm password" 
                                    required minlength="8" 
                                    maxlength="24"
                                    id="repeat_password">
                                    <span asp-validation-for="ConfirmPassword"></span>
                                </div>
                            </div>
                            <div class="register__form__buttons text-center">
                                <button 
                                class="btn btn--big btn--long" 
                                type="submit">
                                    Sign Up
                                </button>
                                <div class="register__divider">or</div>
                                <a class="btn btn--big btn--long" 
                                asp-controller="Auth" 
                                asp-action="Login"
                                asp-route-returnUrl="@Model.ReturnUrl">
                                    Sign In
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>
</body>
</html>